# Application dependencies

require:
    - play
    
    # FAP 
    - fap -> fap 1.0
    
    # Play modules dependencies
    - play -> pdf 0.6
    - play -> less 0.3

repositories:
    - Fap Local repository:
        type:       local
        artifact:   ${fapsdk}/fap
        contains:
            - fap -> *
#    - Fap repository:
#        type: http
#        artifact: "http://fap-devel.etsii.ull.es/public/fap-sdk2/[revision]/[module]-[revision].zip"
#        contains:
#            - fap -> fap

